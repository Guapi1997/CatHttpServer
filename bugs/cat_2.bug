场景：
    火狐浏览器持续无法完成刷新
    压力测试经常无故卡在某个响应

动作：
    点击火狐，打开地址
    http_load -p 10 -s 10 

BUG：
    目前查明的bug： http 响应包的 Context-length 不准确，导致接收放在持续等待；


排查方案：
    输出 发送的响应包和已发送的字节数

最终问题的解决：
    sprintf 最好还是两个字符数组之前的操作，一个很容易产生问题

